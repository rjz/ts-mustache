#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const minimist_1 = __importDefault(require("minimist"));
// Shim for internal compatibility
const facilities_1 = require("../facilities");
const args = (0, minimist_1.default)(process.argv.slice(2));
function usage() {
    console.log(`usage: ${process.argv[1]} --dir=<template dir> -o ./templates.ts

options:
  --dir        the path to the mustache template directory
  -h           print these instructions and exit
  -o           the output path to write the decalaration file
  --node       optionally configure declaration file for use with node.js
`);
    process.exit(2);
}
if (args.h || !args.dir || !args.o) {
    usage();
}
const loader = new facilities_1.DefaultLoader({ dir: args.dir });
const declarer = new facilities_1.Declarer(loader);
declarer.declare().then((declarations) => {
    const header = [
        `/**
*  Generated by ts-mustache at ${new Date().toISOString()}
*
*  @see {@link https://github.com/rjz/ts-mustache/}
*/`,
    ];
    const footer = [];
    if (args.node && args.o) {
        header.push("import path from 'path'");
        footer.push(`export const TEMPLATE_DIR = path.resolve(__dirname, '${path_1.default.relative(path_1.default.dirname(args.o), path_1.default.resolve(args.dir))}')`);
    }
    const output = [...header, declarations, ...footer]
        .filter(Boolean)
        .join('\n\n');
    if (args.o) {
        fs_1.default.writeFileSync(args.o, output);
    }
    else {
        console.log(output);
    }
    process.exit(0);
});
//# sourceMappingURL=declarations.js.map